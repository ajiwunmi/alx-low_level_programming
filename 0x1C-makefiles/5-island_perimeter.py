#!/usr/bin/python3
""""""
def island_perimeter(grid):
    heigth = len(grid)
    width = len(grid[0])

    if(heigth < 100 or width < 100):
        for i in range(0, heigth):
            
        